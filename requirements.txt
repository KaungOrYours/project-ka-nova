# ============================================================
# Project Ka-Nova — requirements.txt
# Phase 1 + Phase 2 unified dependencies
#
# Install: pip install -r requirements.txt
# CRITICAL: Never `brew install mesa` — that installs the 3D graphics
#           library. Use pip install mesa==2.3.0 only.
# ============================================================

# ── CORE ABM FRAMEWORK ──────────────────────────────────────
mesa==2.3.0

# ── DATA & NUMERICS ─────────────────────────────────────────
numpy>=1.24.0
pandas>=2.0.0
polars>=0.19.0
scipy>=1.11.0

# ── VISUALISATION ───────────────────────────────────────────
matplotlib>=3.7.0
seaborn>=0.12.0
plotly>=5.15.0

# ── PROGRESS & LOGGING ──────────────────────────────────────
tqdm>=4.65.0

# ── PHASE 2: LLM ELITE AGENTS ───────────────────────────────
# LangChain stack for Chancellor / President / General LLM agents
langchain>=0.3.0
langchain-core>=0.3.0
langchain-community>=0.3.0
langchain-openai>=0.3.0
langchain-text-splitters>=0.3.0
openai>=1.0.0

# ── PHASE 2: CRYPTOGRAPHIC JUSTICE (Article 15) ─────────────
cryptography>=41.0.0

# ── STATISTICAL ANALYSIS (kpi.py) ───────────────────────────
statsmodels>=0.14.0
pingouin>=0.5.3

# ── SENSITIVITY ANALYSIS (Phase 3 planned) ──────────────────
# SALib>=1.4.7

# ── UTILITIES ───────────────────────────────────────────────
python-dotenv>=1.0.0
pydantic>=2.0.0
typing-extensions>=4.7.0